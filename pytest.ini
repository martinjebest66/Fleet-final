[pytest]
# Default run = the unit suite, which needs no server and no database.
testpaths = backend/tests
python_files = test_*.py
asyncio_mode = auto
addopts = -q --strict-markers
markers =
    integration: requires a running Fleet Manager instance (set FLEET_BASE_URL)
filterwarnings =
    ignore::DeprecationWarning:mongomock.*
