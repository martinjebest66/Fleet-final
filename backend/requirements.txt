# Fleet Manager backend — runtime dependencies.
#
# Trimmed to what the application actually imports. The previous list was a
# `pip freeze` of a development sandbox: ~120 packages including boto3,
# pandas, litellm, stripe, the Google AI SDKs and the linters. It also pinned
# `emergentintegrations==0.1.0`, which is not published on PyPI, so
# `pip install -r requirements.txt` — and therefore `docker compose build` —
# failed outright.
#
# Development and test tooling lives in requirements-dev.txt.

# --- Web framework ---
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
python-multipart==0.0.22        # multipart/form-data uploads (File/Form)
pydantic==2.12.5

# --- Database ---
motor==3.3.1                    # async MongoDB driver
pymongo==4.5.0                  # required by motor; pinned to a compatible line

# --- Auth ---
PyJWT==2.12.1
bcrypt==4.1.3

# --- HTTP client (Emergent OAuth, ICS calendar feeds) ---
httpx==0.28.1

# --- Configuration ---
python-dotenv==1.2.2

# --- Reports & imports ---
reportlab==4.5.1                # logbook PDF export
beautifulsoup4==4.12.3          # reservation export (HTML table saved as .xls)

# --- Notifications ---
resend==2.34.0                  # optional; inactive without RESEND_API_KEY

# --- Time zones ---
# Europe/Prague conversions via zoneinfo need a tz database, which slim base
# images do not ship.
tzdata==2025.3
